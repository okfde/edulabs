comments:
  allowedFields     : ['name', 'email', 'url', 'message', 'replying_to']
  branch            : "master"
  commitMessage     : "New comment."
  filename          : "comment-{@timestamp}"
  format            : "yaml"
  moderation        : true
  path              : "_data/comments/{options.slug}"
  requiredFields    : ['name', 'message']
  transforms:
    email           : md5
  generatedFields:
    date:
      type          : "date"
      options:
        format      : "iso8601"

projects:
  allowedFields     : ['title', 'content', 'short-desc', 'needs', 'lab', 'status', 'link', 'contact-name', 'contact-email']
  branch            : "master"
  commitMessage     : "New project"
  filename          : "{fields.slug}"
  format            : "frontmatter"
  moderation        : true
  path              : "_project"
  requiredFields    : ['title', 'short-desc', 'lab', 'status', 'contact-name', 'contact-email']
  transforms:
    content         : frontmatterContent
  generatedFields:
    date:
      type          : "date"
      options:
        format      : "iso8601"
    slug:
      type          : "slugify"
      options:
        field       :  title

dw:
  allowedFields     : ['title', 'titel', 'art', 'kmk', 'fach', 'medien', 'desc', 'desc2', 'lernziele', 'tipp', 'long-fach', 'zielgruppe', 'lizenz', 'dauer', 'vorbereitungszeit', 'author-name', 'author-email', 'author-website', 'tags', 'link']
  branch            : "dw2"
  commitMessage     : "New tool"
  filename          : "{fields.title}"
  format            : "frontmatter"
  moderation        : true
  path              : "_dw"
  requiredFields    :  ['title', 'titel', 'art', 'fach', 'medien', 'desc', 'desc2', 'lernziele', 'tipp', 'zielgruppe', 'lizenz', 'dauer', 'author-name', 'author-email', 'author-website']
  transforms:
    desc2         : frontmatterContent
  generatedFields:
    date:
      type          : "date"
      options:
        format      : "iso8601"
    slug:
      type          : "slugify"
      options:
        field       :  title


