comments:
  allowedFields     : ['name', 'email', 'url', 'message', 'replying_to']
  branch            : "project-creator"
  commitMessage     : "New comment."
  filename          : "comment-{@timestamp}"
  format            : "yaml"
  moderation        : true
  path              : "_data/comments/{options.slug}"
  requiredFields    : ['name', 'message']
  transforms:
    email           : md5
  generatedFields:
    date:
      type          : "date"
      options:
        format      : "iso8601"

projects:
  allowedFields     : ['project-name', 'project-description']
  branch            : "project-creator"
  commitMessage     : "New project."
  filename          : "project-{@timestamp}"
  format            : "frontmatter"
  moderation        : true
  path              : "_data/projects/{fields.project-name}"
  requiredFields    : ['project-name', 'project-description']
  generatedFields:
    date:
      type          : "date"
      options:
        format      : "iso8601"
